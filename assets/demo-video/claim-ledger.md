# Zuun Demo Claim Ledger

| On-screen claim | Exact meaning | Evidence command/file | Observed value | Safe caption |
| --- | --- | --- | --- | --- |
| Zuun turns durable decisions into local memory | Captured entries are written to local `ZUUN_HOME` and retrievable later | `assets/demo-video/proof/02-capture.txt`; `assets/demo-video/proof/07-files.txt` | Three durable entries written under `/tmp/zuun-video-store/entries` | Zuun turns durable decisions into local memory |
| Memory starts as a file | Capture writes markdown entry files in the store | `assets/demo-video/proof/07-files.txt`; `/tmp/zuun-video-store/entries/ENT-260610-2A35.md` | `ENT-260610-2A35.md` exists alongside `index.db` and `log.jsonl` | A capture writes markdown first, then updates the SQLite index |
| Not every note belongs in memory | Short untagged decisions trigger a warning and can be forgotten | `assets/demo-video/proof/03-lint.txt`; `assets/demo-video/proof/03b-forget-lint.txt` | `ENT-260610-0FE4` warned as likely accidental and was forgotten | Zuun warns on low-quality capture and provides a cleanup path |
| Search explains why a memory surfaced | `zuun explain` prints score components for the result | `assets/demo-video/proof/04-explain.txt` | `fts: 1.000`, `vec: 0.000`, `recency: 1.000`, score `1.000` for `ENT-260610-2AFB` | Results expose FTS, vector, recency, and final score |
| The next agent starts with context | SessionStart hook emits additional context for the current project | `assets/demo-video/proof/05-session-start.txt` | JSON contains three entries in `hookSpecificOutput.additionalContext` | SessionStart injects three high-signal entries |
| Plain files, derived index, inspectable log | The store contains entry markdown, SQLite DB, and JSONL log | `assets/demo-video/proof/06-doctor.txt`; `assets/demo-video/proof/07-files.txt` | `entries on disk: 3`, `entries in db: 3`, `broken related refs: 0`; files include `index.db` and `log.jsonl` | Doctor checks disk, database, refs, audit cadence, and optional embeddings |
| Embeddings are optional | Search/doctor still function when Ollama is down | `assets/demo-video/proof/06-doctor.txt`; `assets/demo-video/proof/04-explain.txt` | Doctor reports `ollama: down` and `note: embeddings are optional; FTS still works`; explain returns FTS result | Embeddings are optional; FTS still works |
| Two MCP tools and one reflection habit | Repo exposes `remember`, `context_for`, and `/zuun:reflect` | `src/mcp.ts`; `skills/reflect/SKILL.md`; `.mcp.json` | MCP server registers `remember` and `context_for`; reflect skill exists and is user-invoked | Two MCP tools. One reflection habit |

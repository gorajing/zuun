# Zuun Demo Claim Ledger

| On-screen claim | Exact meaning | Evidence command/file | Observed value | Safe caption |
| --- | --- | --- | --- | --- |
| Stop re-explaining yourself | Zuun is intended to carry durable decisions from one AI-assisted session into a later one | `README.md`; `src/mcp.ts`; `src/hook-scripts/session-start.ts` | README describes persistent memory; MCP exposes `remember`/`context_for`; SessionStart emits `additionalContext` | Stop re-explaining yourself |
| Capture one durable claim | A manual capture saves one self-contained entry with kind and tags | `assets/demo-video/proof/02a-capture-decision.txt` | Capture returned `ENT-260612-1F49` | One durable claim enters the memory loop |
| Store: markdown files stay primary | Captures write entry markdown files and maintain a SQLite index | `assets/demo-video/proof/07-files.txt`; `src/capture.ts`; `src/lib/store.ts` | Three `ENT-*.md` files exist alongside `index.db` and `log.jsonl` | Markdown files stay primary; SQLite is a rebuildable index |
| Retrieve: Zuun explains why a memory surfaced | `zuun explain` prints score components for a matching entry | `assets/demo-video/proof/04-explain.txt` | `ENT-260612-675D` surfaced with `fts: 1.000`, `vec: 0.000`, `recency: 1.000`, score `1.000` | Zuun explains why a memory surfaced |
| Resume: SessionStart injects high-signal project context | The SessionStart hook emits `hookSpecificOutput.additionalContext` for the current project | `assets/demo-video/proof/05-session-start.txt`; `assets/demo-video/proof/06-doctor.txt` | JSON contains three entries; doctor log reports `session_start.inject` with `hits: 3` and `chars: 435` | SessionStart injects high-signal project context |
| Local markdown + SQLite + SessionStart | The demo is grounded in local store files, derived DB state, and hook output | `assets/demo-video/proof/06-doctor.txt`; `assets/demo-video/proof/07-files.txt` | Doctor reports `entries on disk: 3`, `entries in db: 3`, `broken related refs: 0`; files include `index.db` and `log.jsonl` | Local markdown + SQLite + SessionStart |
| Small surface area | Zuun keeps the agent-facing memory API intentionally small | `src/mcp.ts`; `skills/reflect/SKILL.md`; `.mcp.json` | MCP registers `remember` and `context_for`; the reflect skill describes the reflection habit | remember, context_for, and a reflection habit |
| Embeddings are optional | Search/doctor still run when Ollama is unavailable | `assets/demo-video/proof/06-doctor.txt`; `assets/demo-video/proof/04-explain.txt` | Doctor reports `ollama: down` and notes FTS still works; explain returns an FTS-backed result | Embeddings are optional; FTS still works |
